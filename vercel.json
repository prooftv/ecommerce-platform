{
  "framework": "nextjs",
  "installCommand": "npm install && npm install --prefix apps/storefront",
  "buildCommand": "cd apps/storefront && npm run build",
  "outputDirectory": "apps/storefront/.next"
}
