{
  "name": "ecommerce-platform",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": "22.x",
    "pnpm": "9.15.9"
  },
  "scripts": {
    "dev": "turbo dev --filter=storefront",
    "dev:ops": "turbo dev --filter=operations",
    "build": "turbo build",
    "build:storefront": "turbo build --filter=storefront",
    "build:operations": "turbo build --filter=operations",
    "lint": "turbo lint",
    "test": "turbo test --filter=storefront",
    "test:e2e": "turbo test:e2e --filter=storefront"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  }
}
